{
  "name": "node-data",
  "version": "0.6.0",
  "author": "Endri Gjiri",
  "description": "A library to standardize api calls and responses",
  "license": "MIT",
  "private": false,
  "repository": "git@github.com:zoocasa/node-data.git",
  "scripts": {
    "lint": "eslint .",
    "clean": "rm -rf dist",
    "build": "yarn clean && tsc",
    "watch": "tsc -w",
    "start": "yarn watch",
    "test": "tsc && qunit dist",
    "test:live": "nodemon -w ./dist --exec 'clear && qunit dist'"
  },
  "dependencies": {
    "egjiri-node-kit": "^0.11.0",
    "isomorphic-unfetch": "^3.0.0",
    "jquery-param": "^1.0.2"
  },
  "devDependencies": {
    "@types/node": "^12.0.12",
    "nodemon": "^1.18.3",
    "qunit": "^2.6.1",
    "ts-node": "^8.3.0",
    "typescript": "^3.0.1"
  }
}
